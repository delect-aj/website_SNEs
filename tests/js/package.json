{
  "name": "microbial-embeddings-website-tests",
  "private": true,
  "type": "module",
  "description": "Regression tests for the browser-side preprocessing, inference and table reader.",
  "scripts": {
    "test": "node preprocess.test.mjs && node table.test.mjs"
  },
  "dependencies": {
    "onnxruntime-node": "1.16.3"
  }
}
