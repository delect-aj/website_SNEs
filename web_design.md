# SNE 公开访问网站 —— 第一版方案

## Context

论文（Ecological embeddings reveal microbial social niches that generalize across
diseases）已投稿，数据与代码托管在 GitHub
（xu-research-lab/microbial-embeddings）。目前对外只有"数据下载"，访客无法体验
工具本身能做什么，曝光度受限。

目标：做一个公开网站，**既展示 SNE 这个表示学到了什么，也让访客用自己的或示例
数据跑一次真实分析**，同时把服务器算力压到接近零。

三个已定的约束（来自沟通）：

1. 部署资源 = 一台 1-2 核小服务器；
2. 首版范围 = 生态图谱 + 统一失衡评分（单病模型留到第二版）；
3. 结果定位 = 研究用途，输出**相对分位数**，不输出"患病概率"。

## 核心判断

**分类模型的算力不是瓶颈，输入格式才是。**

- 模型极小：1 层 attention、1 head、`d_model=100`、`num_steps` 400~600
  （[otu_attention.py:1388](otu_attention.py#L1388)，Methods L493-504）。非
  embedding 参数约 13 万，单样本前向 < 10 ms。
- 真正贵的是把用户数据映射到 14,093 个 OTU 的词表：q2-vsearch 闭合参考、97%
  相似度、SILVA v138.2（Methods L325-331）。

由此得出方案的两条主线：

1. **分类推到浏览器里跑**（ONNX Runtime Web）。1-2 核的服务器不该花在能被访客
   自己的 CPU 免费承担的事情上，而且访问量涨了也不用扩容。
2. **服务器上只留一件事**：把用户的 rep-seqs 比对到 OTU 词表。关键优化是
   **只比对那 14,093 条参考序列，不是整个 SILVA**——库小三个数量级，几百条 ASV
   秒级完成，1 核够用。

## 架构

```
静态站（nginx / OSS / Pages）        小容器（1 核，唯一的服务端）
├── /atlas      生态图谱             └── POST /map
├── /dysbiosis  失衡评分                  rep-seqs FASTA
│   └── onnxruntime-web 在浏览器跑        → vsearch --usearch_global
├── /download   向量与模型下载             (db = 14,093 条 OTU 参考序列)
└── /cite                                → {asv_id: otu_id} JSON
```

210k 样本的原始大文件放 Zenodo / HuggingFace，不自建带宽。

## 离线预计算产物（在实验室 GPU 机器上跑一次，产出全是静态文件）

| 文件 | 内容 | 体积 |
|---|---|---|
| `sne.f16.bin` | 14093 × 100 预训练 SNE，float16 | 2.8 MB |
| `otus.json` | OTU id、SILVA 分类、代表基因组、Traitar/BacDive 性状 | ~4 MB gz |
| `umap.f32.bin` | 14093 × 2 图谱坐标 | 113 KB |
| `nbr_sne.bin` | 每个 OTU 的 top-50 生态近邻（int16 索引 + f16 余弦） | 2.1 MB |
| `nbr_phylo.bin` | 同结构，但来自 PhyloE（Methods L457-463） | 2.1 MB |
| `traits.f16.bin` | 14093 × 约 20 个性状的 RF 预测概率（见"生态属性预测"） | ~560 KB |
| `traits_meta.json` | 每个性状的留一门交叉验证 AUC、标签数、正样本数 | < 5 KB |
| `bacdive.json` | 有 BacDive 实测记录的 OTU 及其性状，覆盖预测值 | 小 |
| `labeled_idx.json` | 每个性状的已标注 OTU 索引及其标签，用于画可信度分布图 | ~100 KB |
| `dysbiosis.f16.onnx` | 跨 13 病统一模型（Fig.5G），含自带 embedding 表 | ~3 MB |
| `ref_scores.json` | 参考队列（4,387 对照 + 5,889 病例）的 logit 排序数组，用于查分位 | 40 KB |
| `otu_refseqs.fasta` | 14,093 条 SILVA 代表序列（**仅服务端**，vsearch 库） | ~20 MB |
| `examples/*.json` | 若干已预处理好的示例样本（健康 / IBD / CRC） | 小 |

导出脚本：新建 `web/export/` 下的一次性脚本，复用
[otu_attention.py](otu_attention.py) 中已有的
`otuEmbedding`（L1003）、`OtuAttentionEncoder`（L1318）、`Fid`（L251），不要重写。

ONNX 导出：`forward(x, weight, mask)` 三个输入全是标准算子，直接
`torch.onnx.export`，batch 维设 dynamic axis，同时导出第二个输出
`attn_weights`（L1444-1530）——可解释性直接来自它，不需要额外算 SHAP。

## 页面一：生态图谱 `/atlas`（纯静态，零服务端算力）

浏览器加载 2.8 MB 的 SNE 矩阵后，下面全部客户端计算：

1. **UMAP 散点**，14k 点用 WebGL（`regl-scatterplot` 或 deck.gl）。按门 /
   需氧性 / Traitar 性状着色切换。
2. **菌名搜索 → 微生物卡片**：分类、代表基因组、性状、以及
   **生态近邻 vs 系统发育近邻并排对照**。这是首版最该做好的一屏——论文 Fig.4
   的 ρ 只有 -0.44（门）/ -0.21（属），"生态 ≠ 亲缘"一眼可见，也是最容易被人
   截图转发的内容。
3. **未培养 OTU 的生态属性预测**——见下节。14,093 个 OTU 里只有 1,112 个映射到
   了代表基因组（Methods L345），其余约 13,000 个是未培养、无基因组、无表型记录
   的菌。让访客查到这些菌的推断性状，是这个网站唯一别处拿不到的东西，也是让人
   愿意回访第二次的功能。

## 生态属性预测：只用一个预测器

**只做一件事：在 SNE 上训随机森林预测性状，留一门交叉验证**（论文 Fig 2C、
Methods L441-443），每个性状输出一个概率。

### B 的实现：离线算成一张查找表

浏览器里不跑任何模型。14,093 × 约 20 个性状全部离线算好，存成 float16
二进制，约 560 KB，前端只做数组下标查找。

新建 `web/export/export_traits.py`，每个性状两步：

1. **留一门交叉验证求 AUC**，与论文 Fig 2C 同一套流程——训练与验证的菌绝不
   同门（Methods L442-443）。这个 AUC 不是装饰，是整个功能唯一的诚实性保证；
2. **用全部标签重新拟合，推全部 14,093 个 OTU**。

用随机森林而非逻辑回归，因为**论文用的就是它**——站上的数字必须和发表的图对得
上。反正离线算，模型复杂与否无所谓。

标签来自论文已有的两路：Traitar 跑代表基因组（Methods L424-426），BacDive 经
SILVA 登录号查 API 的人工实测表型（Methods L426-430）。

### 展示：AUC 必须和预测值并排

这是这块功能成败的地方。AUC 0.55 的预测和 AUC 0.90 的预测长得一样，就是在误导人。

```
🔬 预测的生态属性                    ⓘ 基于 SNE 推断，非实验测定

  需氧性      专性厌氧   ████████░░ 0.87   ● 可信   留一门 AUC 0.89
  革兰氏染色  阴性       ███████░░░ 0.79   ● 可信   留一门 AUC 0.85
  产芽孢      不产芽孢   ██████░░░░ 0.68   ◐ 中等   留一门 AUC 0.74
  运动性      不运动     █████░░░░░ 0.55   ○ 弱     留一门 AUC 0.63
  ──────────────────────────────────────────────────────────
  另有 6 个性状因交叉验证 AUC < 0.65 未予展示   [展开查看]
```

- **硬规则：AUC < 0.65 的性状默认折叠不显示。** Fig 2C 末尾贴近 0.5 的那几个
  （Trehalose 等）就该被砍掉。宁可少给四个数，不要给一个错的。
- **有实测就不预测**：BacDive 有记录的菌直接显示实测值，视觉上与预测区分
  （`【BacDive 实测】` vs `【预测 0.68 ◐】`）。卡片因此自然成为"已知 + 推断"
  的聚合视图。

### 可信度：把已知菌的预测分布画在同一条概率轴上

单给一个 0.87 没有说服力。展开某个性状时，画出**已标注菌在这同一个模型下的预测
概率分布**，再标出查询菌的位置：

```
P(专性厌氧)      ← 就是上面那个概率，不是第二个数

0 ──────────────────────────────────────────────────► 1
     ○○○○○○○○○                        ●●●●●●●●●●●●●●●
     已知需氧/兼性 (n≈98)               已知专性厌氧 (n≈312)
                                              ▲
                                      OTU_009823（未培养）0.87
```

两组已标注菌的分布是必须画的——没有它们，轴上一个孤零零的点什么也不说明。访客
看到的是完整逻辑链：**"它落在已知厌氧菌那一堆里，所以推断它厌氧"**，不需要懂
随机森林也能接受。

这张图画在预测器自己的输出上，所以不引入第二个数、不可能自相矛盾，也不需要额外
的产出文件——已标注菌的概率本来就在 `traits.f16.bin` 里。

顺带地，这两堆点分得开不开，本身就是那个 AUC 的可视化：**AUC 高的性状两堆分得
开，AUC 低的糊成一团**。折叠 AUC < 0.65 的性状这条规则，在图上是自明的。

### 三条必须写在页面上的限制

1. **预测的是生态位，不是生理学。** SNE 来自共现，"厌氧"部分源于"总和肠道里
   厌氧的东西一起出现"。这正是论文讲的 realized niche，但它意味着结论是
   **该菌在肠道中表现出的角色**，不完全等于纯培养下的生理特性。
2. **只在人肠道内有效。** 210,090 个样本全是人类粪便（Methods L315-331），不
   能外推到土壤、海洋、口腔。
3. **训练集是已培养菌，预测对象是未培养菌**（约 1,100 → 约 13,000，12 倍外
   推），两群菌本身存在系统性差异。留一门交叉验证正是为量化这个外推能力而做，
   所以那个 AUC 必须和预测值同时出现。

## 页面二：失衡评分 `/dysbiosis`

用统一模型（LODO AUC 0.70，未见疾病仍有 0.65，Fig.5G-H）作首屏主打，而不是单
病模型——**任何粪便样本都能出一个数，访客不需要先声明自己得了什么病**，这是最
容易传播的钩子，也正好绕开首版不做 13 个模型的范围限制。

**输入三档**（全部限制样本数与文件大小）：

- (a) 已是 SILVA-138.2 97% OTU id 的 BIOM / TSV → 直接跑，零服务端；
- (b) rep-seqs FASTA + count 表（DADA2 / QIIME2 标准输出）→ 调 `POST /map`；
- (c) **一键跑示例样本**，无需上传。绝大多数访客没有自己的数据，没有这一档等
  于没人用。

**不接受原始 FASTQ。** 算力就死在这一步。

**前端需要复刻的预处理**（这是唯一容易出错的地方，已在本地用 biom 验证过语义）：

1. 每个样本内，**只对非零 count 排秩**（biom 是稀疏的，零值不参与），
   `method='average'` 处理并列；
2. 除以该样本的最大秩 → 落在 (0, 1]；零仍是零
   （[otu_attention.py:70](otu_attention.py#L70) `read_imdb`）；
3. 取丰度最高的 `num_steps` 个 OTU，不足则补 `<pad>`；
4. mask 中 `<pad>` 与 `<unk>` 一律置 0
   （[otu_attention.py:453](otu_attention.py#L453) `truncate_pad`）。

约 40 行 JS。

**输出**（按"研究用途 + 分位数"定位）：

- 主结果：「你的样本的生态失衡程度处于参考队列第 N 百分位」，配一条参考队列
  分布图，标出健康与患病样本的位置。**不出现"概率"「风险」「诊断」字样。**
- 次结果：attention 权重排名靠前的贡献菌，每个可点进 `/atlas` 的卡片页。
- 页脚固定免责声明：仅供科研，非医疗器械，不用于诊断。

## 页面三：下载与引用 `/download`、`/cite`

- SNE 向量导出为 gensim `.kv` 和 TF Projector 的 `.tsv`；
- **直接给一个跳转 projector.tensorflow.org 并加载你们向量的链接**——白嫖一个
  3D 交互浏览器，零开发成本；
- 每个分析挂一个 **Colab 按钮**：重活交给 Google 的免费算力，你们零成本，同时
  这是让人真正上手用起来的路径；
- BibTeX、DOI、版本号、GitHub 链接。

## 服务端：唯一的容器

单个 FastAPI 端点：

```
POST /map   multipart: rep_seqs.fasta
→ vsearch --usearch_global q.fasta --db otu_refseqs.fasta
          --id 0.97 --maxaccepts 1 --blast6out -
→ {"mapping": {asv_id: otu_id}, "mapped": 312, "total": 420}
```

- 限制：单次 ≤ 5,000 条序列、≤ 10 MB、按 IP 限流；
- 同步返回即可，秒级完成，不需要队列；
- 无账号、无数据库、无持久化——请求处理完就删临时文件。

1 核 2 GB 足够。若日后要支持批量样本或原始 FASTQ，再谈升级资源。

## 验证

1. **黄金样本回归测试**（最关键）：挑 20 个样本，用现有 Python 流程
   （[run_attention_biom_with_SNEs.py](run_attention_biom_with_SNEs.py)）算出
   logit，再用浏览器端 JS 预处理 + ONNX 跑一遍，断言差值 < 1e-4。预处理复刻错
   一步整个网站的结果就是错的，这个测试必须先写。
2. `/map` 端点：拿一批已知 OTU 的序列做输入，断言映射回原 OTU id；再喂空文件、
   超限文件、非 FASTA，确认返回 4xx 而不是 500。
3. **性状预测表**：`export_traits.py` 产出的留一门 AUC 必须与论文 Fig 2C 的数
   值一致——对不上说明交叉验证流程复刻错了，那么整张预测表都不可信。再断言前端
   确实折叠了所有 AUC < 0.65 的性状，且 BacDive 实测值覆盖了对应的预测值。
4. 图谱页：断言 14k 点渲染后仍能交互（本地 Lighthouse 跑一次），2.8 MB 矩阵在
   慢网络下有加载态。
5. 端到端手测：示例样本一键跑通 → 分位数出现 → 点贡献菌跳进卡片页 → 卡片页显
   示生态近邻、系统发育近邻与带 AUC 的预测性状。

## 明确不做（第二版再说）

- 13 个单病模型与 IBD 亚型分类；
- 原始 FASTQ 上传；
- 用户账号、结果保存、历史记录；
- 服务端渲染、数据库。

---

# 设计风格

## 北极星

**像一篇排版良好的方法学论文，不像一个健康 App。**

这一条决定后面所有取舍。访客拿到的是分位数、AUC、置信度，不是诊断结论；界面
必须在视觉上先说出这件事，再让免责声明去补充。任何让人联想到体检报告的元素
（红黄绿仪表盘、笑脸/哭脸、"您的健康得分"式大数字圆环）一律不用——不是因为
丑，是因为它们会覆盖掉正文里那三条限制。

## 三条设计原则

1. **不确定性和数字同等醒目。** 概率永远不单独出现，一定和 AUC、和已标注菌的
   分布图同屏。视觉层级上 AUC 不是脚注字号。
2. **颜色不承载唯一信息。** 色盲、截图转灰度、投影仪都会毁掉纯颜色编码。置信
   度靠 ● ◐ ○ 字形 + 填充密度，实测/预测靠边框实虚，颜色只做锦上添花。
3. **零字体下载。** 页面预算 2.8 MB 全给 SNE 矩阵，不给 webfont。系统字体栈，
   首屏立即可读。

## Design tokens

```css
:root {
  /* 纸与墨 */
  --paper:    #FBFAF8;   /* 页面底色，暖白，长时间阅读不刺眼 */
  --surface:  #FFFFFF;   /* 卡片 */
  --ink:      #16181C;   /* 正文 */
  --ink-2:    #5A6068;   /* 次要文字、单位、注释 */
  --ink-3:    #9AA0A6;   /* 禁用、占位 */
  --line:     #E4E2DD;   /* 分隔线，1px */
  --accent:   #1F5F8B;   /* 链接、主按钮、选中态 */
  --accent-bg:#E8F0F5;

  /* 图谱画布：唯一的深色区域 */
  --well:     #12161B;
  --well-ink: #C9D1D9;
  --well-line:#2A313A;

  /* 参考队列两组（Okabe-Ito，避开红绿） */
  --ctrl:     #0072B2;   /* 对照 */
  --case:     #D55E00;   /* 病例 */
  --you:      #16181C;   /* 你的样本 = 墨色，永远比两组更重 */
}
```

**分类配色一律用 Okabe-Ito 八色**（门、需氧性、性状着色切换共用一套）：
`#0072B2 #D55E00 #009E73 #CC79A7 #F0E442 #56B4E9 #E69F00 #999999`。
超过八类的门归入"其他 #999999"，不发明第九个颜色。

**连续量**（性状概率、余弦相似度）用 `--accent` 单色明度梯度，不用 viridis
彩虹——单色梯度在灰度下仍然单调，彩虹不是。

## 字体与数字

```css
--font: system-ui, -apple-system, "Segoe UI", "PingFang SC",
        "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
--mono: ui-monospace, SFMono-Regular, "JetBrains Mono", Consolas, monospace;
```

- 正文 15px / 1.55，密度偏紧——这是工具不是落地页；
- **所有 OTU id、序列、AUC、概率、分位数用 `--mono` + `font-variant-numeric:
  tabular-nums`**。等宽是让一列 AUC 能被竖着扫读的唯一办法；
- 标题只用三级：28 / 19 / 15，全部 600 字重，不用 700 以上；
- 中文不做斜体，强调用 `--ink` 加粗或 `--accent-bg` 底色。

## 布局

- 内容区 `max-width: 1120px`，单栏，左对齐（不居中正文——长行居中读起来累）；
- `/atlas` 例外：全幅两栏，左边 WebGL 画布自适应，右边微生物卡片固定 400px，
  <1024px 时卡片变为覆盖式底部抽屉；
- 卡片：`--surface` + 1px `--line`，圆角 6px，**不用阴影**。阴影是 App 语言，
  细边框是文献语言；
- 所有间距取 4 的倍数，段落之间 16，区块之间 32。

## 关键组件

### 性状条（`/atlas` 卡片核心）

```
需氧性      专性厌氧   ████████░░ 0.87   ● 可信   留一门 AUC 0.89
```

- 条形填充色 = `--accent` 明度梯度；
- 置信度三档只用**填充密度 + 字形**：● 实心（AUC≥0.80）／◐ 半填充
  （0.65–0.80）／○ 斜线纹（<0.65，默认折叠）。不给三档配三种颜色；
- AUC 与概率同字号同颜色，只是靠右——它不是脚注；
- **实测 vs 预测靠边框区分**：`【BacDive 实测】` 用 2px 实心左竖线 + 无进度条
  （实测没有概率，不该画一根 100% 的条）；`【预测】` 用 1px 虚线左竖线。

### 分布带（展开后的可信度图）

一条 0→1 的轴，两组已标注菌用 `--ctrl` / `--case` 的半透明抖动点，查询菌用
`--you` 的实心三角从下方顶上来。两组各自标 n。轴上不放渐变背景——背景渐变会
让人误以为"越右越危险"。

### 分位数结果（`/dysbiosis` 主结果）

- 主句是**一行文字**，不是一个大数字圆环：
  「你的样本处于参考队列第 **73** 百分位」，73 用 32px mono 加重，其余 19px；
- 底下一条参考队列分布图，配色与分布带完全一致——两个页面用同一套视觉语法，
  访客只需要学一次；
- 分布图**不分区着色**（不涂"健康区/患病区"），只画两组的实际分布 + 你的位置；
- 免责声明用 `--ink-2`、13px，常驻在结果正下方而不是页脚——页脚的没人看。

### 生态近邻 vs 系统发育近邻

左右两列并排，**同宽同高同排版**，只有列头不同。这一屏是最容易被截图转发的，
任何让两列看起来不对等的设计（一边大一边小、一边彩色一边灰）都会削弱
"生态 ≠ 亲缘"的说服力。两列中出现的同一个菌用 `--accent-bg` 底色高亮，让重叠
（或不重叠）一眼可见。

## 深色画布

只有 `/atlas` 的散点区用 `--well` 深底：14k 个点在深底上才有对比度。卡片、
表单、正文一律浅色。**不做全站暗色模式**——两套配色 = 两套图表颜色要验证，
第一版不值得。

## 状态

- **2.8 MB 矩阵加载**：骨架屏 + 真实字节进度（`Content-Length` 已知），不用
  转圈。慢网络下访客需要知道还要等多久，不是"还在转";
- 空态、错误态用文字说清楚下一步（"这个文件不是 FASTA，需要 rep-seqs"），不
  用插画；
- `POST /map` 返回 4xx 时，把 `mapped/total` 也显示出来——映射率低本身是有用
  信息。

## 动效

120–180ms `ease-out`，只用于卡片切入和展开折叠。散点图的缩放平移跟手，不加
惯性动画。全部包在 `@media (prefers-reduced-motion: reduce)` 里可关闭。

## 无障碍底线

- 正文对比度 ≥ 7:1，次要文字 ≥ 4.5:1；
- 散点图点必须可键盘 Tab 到（至少搜索结果高亮的那个），卡片信息不能只存在于
  hover tooltip 里；
- 所有图表配一个可展开的数据表 `<details>`——屏幕阅读器读不了 canvas。

## 不做

- 全站暗色模式；
- 自定义字体、图标字体库（图标用内联 SVG，需要几个画几个）；
- 首页 hero 大图、动画背景、滚动视差；
- 组件库（Material / Ant）。整站不到十种组件，一份 400 行的 CSS 够了。
